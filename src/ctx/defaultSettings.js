export default {

  /**
   * If true, broadcast will take effect after the current event
   * loop tick (basically be wrapped with setSimeout(..., 0)). Default is false
   */

  deferExecution: false,
}