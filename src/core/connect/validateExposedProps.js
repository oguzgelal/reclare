export default exposedProps => {
  if (!exposedProps || typeof exposedProps !== 'object') {
    fail(
      'The first argument of connect must be a function and return object of key / value pairs',
      'AF8tNMQpak6LU0'
    );
  }
};
